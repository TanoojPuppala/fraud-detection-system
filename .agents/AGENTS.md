# Project Rules & Workflows

- **Git Workflow**: Always stage (`git add`), commit with a descriptive message, and push (`git push origin main`) to GitHub after completing code modifications or creating reports.
